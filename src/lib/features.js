/**
 * Feature toggles for Corizo Desk.
 *
 * Departments are hidden for now (single-org EdTech flow).
 * To restore department UI + selection everywhere, set ENABLE_DEPARTMENTS to true.
 */
export const ENABLE_DEPARTMENTS = false;
